import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'restaurant-owner-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
